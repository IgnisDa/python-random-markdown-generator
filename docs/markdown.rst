The Core Markdown Generator
===========================

.. automodule:: mdgen.markdown

MarkdownGenerator overview
--------------------------

A :class:`MarkdownGenerator` is a base class that all other valuable classes,
such as :class:`MarkdownOutputGenerator` and :class:`MarkdownPostGenerator` inherit from.

Here's what the `MarkdownGenerator` classes looks like:

.. autoclass:: MarkdownGenerator
    :members:
